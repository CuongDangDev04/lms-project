{
  "name": "project-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "fe:dev": "npm --prefix ./client run dev",
    "fe:build": "npm --prefix ./server run build",
    "fe:lint": "npm --prefix ./fe run lint",
    "fe:preview": "npm --prefix ./fe run preview",
    
    "be:dev": "npm --prefix ./be run dev",
    "be:start": "npm --prefix ./be run start",
    "be:test": "npm --prefix ./be run test"
  }
}
